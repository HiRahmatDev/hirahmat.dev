import { BackToHomeButton } from "@/app/(main)/components/BackToHomeButton";

# Test MDX Page (H1)

This page is render from .mdx file (p).

This is component from direct `import { BackToHomeButton } from "@/app/(main)/components/BackToHomeButton";` in this .mdx file:

<BackToHomeButton />
